{
  "name": "mapscrapperleads",
  "version": "1.0.0",
  "description": "AI agent that generates and publishes LinkedIn posts from map-scraped leads",
  "type": "module",
  "scripts": {
    "dev": "tsx src/index.ts",
    "dev:dry": "tsx src/index.ts --dry-run",
    "build": "tsc",
    "start": "node dist/index.js",
    "start:dry": "node dist/index.js --dry-run"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.39.0",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
